# Rhytmic Exam Workspace

The application has been moved under:

- `flask_backend/`

Quick start from this repository root:

```bash
cd flask_backend
pip install -r requirements.txt
flask --app rhytmic run
```

For full documentation, see `flask_backend/readme.md`.
